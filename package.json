{
  "name": "halom-protocol",
  "version": "1.0.0",
  "description": "Halom Protocol - Fourth root based DeFi system",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.14.4",
    "schedule": "^1.0.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.1"
  }
}
